# graphify
- **graphify** (`~/.claude/skills/graphify/SKILL.md`) - any input to knowledge graph. Trigger: `/graphify`
When the user types `/graphify`, invoke the Skill tool with `skill: "graphify"` before doing anything else.

# Change Propagation (STANDING ORDER, Shaheen 2026-07-01, ALWAYS)
Before any conversation clear / at the end of any work session that changed something real, propagate the change across EVERY connected file, never just the one you touched. That means: the relevant infrastructure/runbook files, the project/global CLAUDE.md, the local project CLAUDE.md, the index, the log, project status pages, routing tables, vault/identity.md (the personal-os system compendium, added 2026-07-02), and anything else linked to the change. No change is "done" until its whole documentation surface reflects it. This is not optional and not something to be reminded of.
