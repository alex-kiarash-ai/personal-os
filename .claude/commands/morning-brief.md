# /morning-brief - Daily 8:00 Inbox + Calendar + Context Brief

Spec: work/02-morning-brief/CLAUDE.md (read it first).

## Steps
1. Read vault/projects/morning-brief/status.md for the Daily Briefs DB IDs.
2. Gmail: `search_threads` with `is:unread in:inbox newer_than:1d`, trim to last 12h by timestamp (Gmail has no hour granularity in search).
3. Calendar: `list_events` for today, `startTime`/`endTime` ISO 8601, `timeZone: Europe/Stockholm`. Treat FROM_GMAIL all-day transparent events as context, not meetings.
4. Notion/vault: pull context on senders, attendees, companies from vault/people/, vault/business/, vault/projects/*/status.md; notion-search if vault is silent.
4b. **HQ inbox check (two-way notes, build #16; full runbook = work/16-alex-hq/CLAUDE.md "Inbox Contract").** Fetch: `curl -s -m 10 https://n8n.shaheenkiarash.com/webhook/alex-inbox -H "X-Alex-Token: $(cat work/16-alex-hq/config/alex-hq-token.txt)"`. `count_new` 0 → skip; unreachable → one FYI line, never fail the brief; never print the token. **Voice notes** (source `hq-voice`): `scp n8n:/opt/alex-inbox-audio/{audio}` to the session scratchpad → transcribe with LOCAL Whisper (`whisper <file> --model base --output_format txt`, #06 protocol) → transcript = the note text. **File each note per the standing vault protocols** (dated event → Google Calendar; person → People Intake; goal/preference → vault/me/; meeting → vault/meetings/; project → vault/projects/; else best-fit page), [[links]] + vault/log.md as usual - scheduled runs never block on questions: file best-guess, tag `data-gap`. **Mark filed** (note REQUIRED - original text for typed, transcript for voice): `curl -s -m 10 -X POST https://n8n.shaheenkiarash.com/webhook/alex-inbox-mark -H "Content-Type: application/json" -H "X-Alex-Token: $(cat work/16-alex-hq/config/alex-hq-token.txt)" -d '{"marks":[{"id":{id},"filed_to":"{short destination}","note":"{final text}"}]}'`. After a successful voice mark: `ssh n8n "rm -f /opt/alex-inbox-audio/{audio}"` + delete the local copy. The brief gets a short **"Notes you dropped"** block: each note → where it went. **Corrections (build #22 Teach-Alex):** if a note is a correction ("you got X wrong, the rule is Y"), also classify it (voice/fact/person-label/rule/format) and append it to vault/projects/teach-alex/corrections-log.md, applying per #22 (identity files - soul.md, any CLAUDE.md - CONFIRM before applying). It still shows in the block as filed → corrections-log.
4c. **Mondays:** include the Radar section per the spec (latest vault/projects/alex-ai-radar/radars/ file; a missing Monday sweep gets flagged in Key Context, never silently skipped).
4d. **Life Ops section (every brief, added 2026-07-02).** Google Drive MCP `read_file_content` on file id `1BedopTrI-Aoh1c9_-RxmMzIXjHmpHuWSpV15pBAfUNw` (Plant Watering Schedule; tabs = plants / GymSchedule / LearningSchedule). Build a short **Life Ops** block per the spec: plants due = Last Watered + frequency vs today (ignore the sheet's Next Water Date/Status columns; if ALL plants are >2x frequency overdue, print the one-line stale-sheet flag instead of the full list); gym = session day if (today − StartDate) is an even number of days, else rest day; learning = today's weekday blocks in Order with hours. Sheet unreachable → one FYI line, never a failed brief. Read-only: the brief never writes to the sheet - but if an HQ note (step 4b) says "watered plants"/"gym restarts {date}", file it by POSTing the Life Ops write-back webhook (`/webhook/life-ops-update`, X-Alex-Token, `{"watered":"all","watered_date":"...","gym_start":"..."}`; runbook docs/n8n/life-ops-sheet-writeback/).
4e. **Body / Health line (build #17 health-tracker, added 2026-07-04).** Fetch the HQ summary once: `curl -s -m 10 https://n8n.shaheenkiarash.com/webhook/alex-hq-summary -H "X-Alex-Token: $(cat work/16-alex-hq/config/alex-hq-token.txt)"` (same token, never printed). Read `projects.health.metrics`: `steps_today.value_num` = yesterday's COMPLETE steps; `sleep_score_today.value_num` = the Alex Sleep Score 0-100 with `headline` carrying the night date + hours. Add ONE line to the Life Ops block: **Body: {steps:,} steps yesterday · Sleep {score}/100 ({hours}, night of {date})**. Label sleep by its night date, do NOT call it "last night" unless that date is actually last night (the 23:59 pull can run a night behind if a run is skipped). Optional nudge if steps <5k or score <70. Missing/unreachable `health` block → skip the line + one FYI ("health not synced"), never fail the brief. Read-only.
4f. **Mondays - Recovery sweep (build #18 recovery-layer, added 2026-07-04).** Read `vault/projects/recovery/last-sweep.md` (rewritten by the Monday 07:30 `PersonalOS-recovery-check` deterministic sweep). If the result is CLEAN, one quiet line ("Recovery: system consistent"). If it found drift, surface the top categories + counts as a **Key Context** line (e.g. "Recovery: 23 drift items - 3 orphan projects, 3 stale-status, 135 dangling links") and point to the report. If the file is missing or its timestamp is not today's Monday, flag "recovery sweep did not run" in Key Context (never silently skip). Read-only; the sweep detects, Shaheen triages.
4g. **Carry-overs / last night's plan (added 2026-07-05, Shaheen's request).** Read `vault/me/carry-overs.md`. Surface its **## OPEN** items as a short **"Carry-overs"** block near the TOP of the brief - one tight line per open item (what Shaheen + Alex agreed to continue, with its next action). This is the first thing Shaheen wants to see on waking ("here's what we said we'd continue from last night"). Timing rule: it's for the next working day, else Monday, so if today isn't a working day, still show it but note "for Monday." Skip-safe: no file or no OPEN items → skip. (This is the seed of a fuller feature: a proper brief block + an Alex HQ website tile, open item #1 in that file.)
4h. **Interview watch (feeds #21 Interview-to-Offer, added 2026-07-06).** Scan the already-fetched Gmail (step 2) + today's calendar (step 3) for interview signals: a Calendar event or Gmail thread that looks like a scheduled interview (words like interview, technical screen, hiring manager; a booked time tied to a company in vault/business/ or a recruiter in vault/people/). If found, add ONE line under Urgent or Key Context: **Interview watch: looks like an interview with {Company} on {date/time} - run `/interview "{Company}"` for a dossier + prep.** Flag ONLY, never auto-run /interview and never draft anything. Nothing found -> no line. Read-only.
4i. **Email-triage signals (feeds #07, added 2026-07-10 v2).** Skip-safe reads of `work/07-email-triage/state/`: (a) `sender-tally.json` suppression candidates → if any sender crossed the threshold, one line "Unsubscribe candidates: {N} senders ({top 3}) - say the word to suppress"; (b) `job-threads.json` stale threads → if a recruiter/CRM thread is awaiting Shaheen's reply > N days, one **Key Context** line "Going stale: {who} unanswered {days}d - reply?". No file or nothing over threshold → skip. Read-only, never auto-acts.
5. Classify per the spec's rules: Urgent / Today's Calendar / Key Context / FYI. Newsletters and promos become one noise count line, never itemized.
6. Write vault/projects/morning-brief/history/YYYY-MM-DD.md (Alex voice, [[wiki links]], scannable < 3 min).
7. Create one row in the Daily Briefs DB: title "Brief YYYY-MM-DD", Date, one-line Summary, Urgent Count, FYI Count, FULL brief as page content.
8. Refresh status.md (last_run, counts).

## Post-Run
- vault/people/ page for every new person (flag unknowns as data gaps).
- vault/business/ page for new companies that matter (not newsletter senders).
- vault/log.md: `## [YYYY-MM-DD HH:MM] morning-brief | {urgent} urgent, {fyi} fyi, {noise} noise`.
- vault/index.md only for new page types.
- **Alex HQ metrics push** (build #16 contract, work/16-alex-hq/CLAUDE.md). Never let a push failure fail the run; never print or log the token:
  `curl -s -m 10 -X POST https://n8n.shaheenkiarash.com/webhook/alex-push -H "Content-Type: application/json" -H "X-Alex-Token: $(cat work/16-alex-hq/config/alex-hq-token.txt)" -d '{"project":"morning-brief","metric_key":"urgent_count","value_num":{urgent},"headline":"{top urgent, <=90 chars}","status":"{red if a hard deadline expires today, else green}"}' || true`
- **Airbnb next-booking refresh (added 2026-07-06).** If the step-2 Gmail scan shows the next upcoming booking CHANGED (new confirmation, cancellation, or the current next booking's check-in passed), also push `airbnb.next_booking` with the same failure-tolerant pattern — `value_text` `"{Guest · Mon D-D}"` or exactly `"no booking"`, headline `"next check-in {date} · confirmed {date}"`, status green. No change seen → no push (the /airbnb-host runs own this key; the brief only keeps it honest between them). Exact shape in .claude/commands/airbnb-host.md.
