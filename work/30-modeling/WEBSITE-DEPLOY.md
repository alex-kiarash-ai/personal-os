# shaheenkiarash.com — Deploy Runbook

**Home: `work/30-modeling/` (project #30).** Written 2026-07-19 into the retired pre-promotion path `work/modeling/`; folded into #30 on 2026-07-20 (that folder no longer exists).

**Discovered + first used 2026-07-19** (closes the run-31 "deploy path UNDOCUMENTED" gap). This is how the modeling website ships. The #30 monthly strategy reviewer (step H, CLAUDE.md) uses this exact path for its `/now` rebuild + `wrangler deploy`.

## What serves the site
- **Cloudflare static-assets Worker** named **`plain-block-545a`** (account `36f1f2149fe6965f741e24c40f29c7aa`, zone `ec8e707d1dac7e1f144bfd5c60bd789a`).
- Custom domain binding: `shaheenkiarash.com -> plain-block-545a` (in Cloudflare → Workers & Pages → Custom Domains). The binding is attached to the worker NAME, so redeploying the same-named worker keeps the domain.
- No bindings, no server script — pure static assets. Originally created via dashboard **upload** (drag-drop). There are 3 other unused workers on the account (black-disk-9cb8, mute-haze-d66d, v1); ignore them.
- Also reachable at `plain-block-545a.shaheen-kiarash.workers.dev` (workers.dev preview URL, enabled by default). Same public content; `<link rel=canonical>` in the HTML points search engines at the apex domain. Optional cleanup: add `"preview_urls": false` to the config to turn the workers.dev URL off.

## Source of truth (local)
- **Live content:** `Desktop\Claude\Modeling\Website-v2-build\` — this folder IS what's deployed. Edit here.
- **Deploy config (kept OUTSIDE the assets folder so it never gets served):** `Desktop\Claude\Modeling\deploy-config\wrangler.jsonc`:
  ```jsonc
  { "name": "plain-block-545a", "compatibility_date": "2026-05-31", "assets": { "directory": "../Website-v2-build" } }
  ```
- **`.assetsignore`** inside the build folder excludes `.wrangler`, `.assetsignore` itself, `*.csv`, `.DS_Store`, `Thumbs.db` from being served. Keep it — it's what stops a stray file going public.
- The old `Desktop\Claude\Modeling\Website\` folder is the PRE-upgrade content (now superseded by the build). Kept as a local rollback copy only.

## To deploy a change
1. Edit files in `Website-v2-build\`.
2. `cd Desktop\Claude\Modeling\deploy-config` then `npx wrangler deploy --dry-run` — confirm the file count and that no stray/private file is listed.
3. `npx wrangler deploy` (needs `npx wrangler login` first if the token expired).
4. **Verify (never "it uploaded" = done):** `curl` the live page and hash-compare to the built `index.html`; check new asset files return 200 and any guard/private file returns 404; render both widths and LOOK; test the og-image share-card.

## Rollback (two ways)
- **Fast:** Cloudflare dashboard → the worker → Deployments → pick a prior version → Rollback. Or `npx wrangler rollback --name plain-block-545a <version-id>`.
- **From disk:** deploy the byte-identical pre-upgrade backup at `Desktop\personal-os-backups\website-2026-07-18\`.
- Version history: pre-upgrade live = `46fb5323-4db2-4fc9-8659-d1d3045f93d0`; the 2026-07-19 upgrade = `07007ec5-f4e4-4db7-a1b5-43df53a228dc`; digitals surgery 2026-07-19 = `a0285185`; digital-6 swap 2026-07-20 = `dcbe495a-8a67-4ef1-9697-73d62abef591`; **portfolio expansion 2026-07-24 = `95486b2a-64e0-41fe-9876-c919c57cd58f`** (grid went 12 -> 26 frames: removed old frames 10+11, added 16 new; pre-change rollback copy of `index.html` + the two removed frames at `Desktop\Claude\Modeling\_backups\portfolio-2026-07-24\`); **portfolio prune + shuffle 2026-07-24 = `883d5ac4-fd09-44c7-9694-dc56ded36a8a`** (removed display frames 17+26 = files portfolio-19+28, then shuffled the remaining 24 into a mixed order; 26-frame pre-change copy + the two removed frames at `Desktop\Claude\Modeling\_backups\portfolio-2026-07-24b\`); **digitals hidden 2026-07-24 = `4a098101-47fd-46e5-9392-bc0eb594b883`** (the /digitals/ page pulled from public view while Shaheen reworks it: nav link removed from all 5 served headers - index + now + the 3 intent pages - `/digitals` dropped from `sitemap.xml`, and `.assetsignore` now excludes `digitals/` + `digital-*.jpg` + `digital-*.webp` so the page AND its loose reference images 404 on the live site. Source files untouched on disk. The `/digitals` route in `work/30-modeling/scripts/scout-checks.mjs` was commented out to match, so Scout's Eye won't false-RED on the intended 404. Restore = revert those edits + re-add the scout route + redeploy. No file backup needed; the change is 3 edits + one deploy); **client swap 2026-07-24 = `a89735f0-8905-47dd-a323-8d90178d9f6a`** (removed Joma, added Shady Rays + Lullevibes as the Selected clients, per Shaheen. Every "Joma" mention scrubbed site-wide: the clients credit line, 5 "· Joma" portfolio captions -> plain "Commercial", 5 alt-texts, the Commercial + Fitness subpage meta/og descriptions, the Fitness keywords, and both subpages' body copy. The 5 tennis frames STAY (photos kept, relabelled generic Commercial) even though the kit is visibly Joma-branded - Shaheen's informed call. New brands are name-credit-only, no portfolio photos attached. First deploy attempt threw a post-upload `fetch failed` network blip but the version still went live; a clean re-run confirmed the trigger deploy and gave this version id. Live-verified: 0 "Joma" on all 3 pages, credit line = "Shady Rays · Lullevibes". Business stubs: [[vault/business/shady-rays]] + [[vault/business/lullevibes]], both needs-review/data-gap); **In Motion film section 2026-07-24 = `13bdbeb0-6899-404d-94f9-867b6a4a9c63`** (published the Lullevibes brand film as a new "03 - Film / In Motion" section between Portfolio and Clients; Contact renumbered to 04; "Film" added to all 5 navs; VideoObject JSON-LD added to the homepage head. The 130 MB / ~20 Mbps source (`Downloads\Lulle Vibes.mp4`, 1920x1080, 53s, with audio) was two-pass re-encoded to `lullevibes-film.mp4` = 18.99 MiB H.264 ~3 Mbps, AUDIO STRIPPED (it's a silent loop), `-movflags +faststart`, comfortably under the 25 MiB Cloudflare per-file cap (confirmed in CF docs). Poster `lullevibes-poster.jpg` cut from the solo 0:32 frame. The section is a lazy-loaded muted autoplay loop: `preload="none"` + an IntersectionObserver starts playback only when the section scrolls into view and pauses it off-screen; `prefers-reduced-motion` shows the poster and never autoplays. Shaheen confirmed the co-models are professional (solo-rule cleared) and rights are cleared before publishing. Live-verified: mp4 200 `video/mp4` + faststart, poster 200, section wired, digitals still 404. Re-encode recipe: `ffmpeg -i src -c:v libx264 -b:v 3000k -pass 1/2 -an -movflags +faststart out.mp4`); **film moved above portfolio 2026-07-24 = `5e9fb055-649d-4ce6-b29e-8083188c6456`** (Shaheen's call - the "In Motion" film section now sits ABOVE the portfolio grid, not below. New section order: About 01 / Film 02 / Portfolio 03 / Contact 04; nav reordered to match, About · Film · Portfolio · Contact, on the homepage + all 4 subpages. Pure section reorder + renumber, no asset change. Live-verified section order + numbering + nav).

## Login note
`wrangler` is installed (v4.112.0). Auth is OAuth via `npx wrangler login` (browser). Token cached at `%APPDATA%\Roaming\xdg.config\.wrangler\config\default.toml` (local-only, gitignored territory — never commit it).
