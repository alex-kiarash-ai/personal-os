# Pattern Library - Research Team

Reusable team architectures from past research runs. Check here BEFORE designing a new team. Format spec in work/04-research-team/CLAUDE.md.

| Pattern | Class | Times used | One-liner |
|---------|-------|------------|-----------|
| [science-explainer](science-explainer.md) | other | 1 | "How/why does X work" question that splits into 2 independent science sub-questions; two parallel lanes + synthesizer that separates fact from folklore. |
| [other-flight-search](other-flight-search.md) | other | 1 | "Cheapest flight" live lookup; no team. Kiwi aggregator with flex-date tiling, parse the saved JSON locally, nonstop = 1 layover entry, filter by stay nights, rank by price. |
| [decision-brief-concept-validation](decision-brief-concept-validation.md) | decision-brief | 3 | "Validate this idea/spec, up/downside, worth building?" Verify only the load-bearing claims the author asserted but didn't check - 3 lanes: cost/viability, prior-art/sellability, core-mechanism (does the "it learns/compounds" claim survive ML/stats reality). Correct your own prior in synthesis. |
| [technical-evaluation-internal-audit-stack-scan](technical-evaluation-internal-audit-stack-scan.md) | technical-evaluation | 4 | "Build X on our own system: what of ours feeds it + what infra signals exist + best external tooling?" Two Explore audit lanes (projects, infra) + one web Stack Scout given an explicit hypothesis to falsify. Synthesizer ranks v1 vs bench and resolves flags with documented judgment calls. Also fits designing a missing system layer (reuse 2: Recovery), hardening a proposed architecture (reuse 3: Health Tracker), and a remote-access feature where the risk is SECURITY not tooling (reuse 4: HQ live conversation - 3 parallel lanes + a sequential adversarial security/QC 4th lane that catches what the transport lanes are naive about). |
| [verification-qa-built-artifact](verification-qa-built-artifact.md) | verification | 2 | "Is this thing I just built actually correct + complete?" Post-build QA of code + its outputs. 3 parallel lanes: static auditor (line-by-line correctness/invariants/portability) + dynamic edge-case tester (isolated fixture, inject one fault per feature) + findings verifier (re-derive outputs, hunt false negatives + self-referential bugs). Synthesize MUST/SHOULD/NICE/DEFERRED. |
| [pitch-build-adversarial-loop](pitch-build-adversarial-loop.md) | other | 1 | "Turn my system/project into a persuasion deck for a specific audience." 3 sequential loops: Loop 1 = 2 parallel research dossiers (infra + cost/value, LOGGED vs ESTIMATE), Loop 2 = QA gap-check from the audience's skeptical seat (recompute every number), Loop 3 = the Judge reads the RENDERED slides + narrative and returns a verdict + ranked design/content fixes. Session synthesizes + builds an animated branded deck between loops. |
| [venture-ideation-validate](venture-ideation-validate.md) | decision-brief | 1 | "Best N near-future business ideas out of this system/person, and are they real?" 4 sequential loops: ideate wide (10-14 candidates) -> identify (weighted score, cut to N complementary winners) -> validate (web-searched build-ready plans: verdict/offer/pricing/competitors/econ/riskiest-test/90-days/kill) -> output QC (read the rendered slides + PDF vs source before ship). Session builds briefing slides + a detailed plan PDF between loops. |
| [technical-evaluation-tool-scout-review-qc](technical-evaluation-tool-scout-review-qc.md) | technical-evaluation | 5 | "Find the best cheap tool/approach for X from the live web, and is the pick actually right?" 3 sequential agents: Scout (web/GitHub/X candidate table, LOGGED vs ESTIMATE costs) -> Reviewer (verify uncertainties, recompute cost, rank shortlist, correct overclaims) -> adversarial QC (SHIP/SHIP-WITH-FIXES, hunt the simpler path we already own). Session grounds with INTERNAL facts first so QC can catch "you already built it." Run 13 flipped adopt->evolve; run 14 variant (research-debate-review); run 16 (voice hands-free) QC deleted a framework dep + caught a TTS language gap; run 17 (upgrade scan) scaled to an 8-lane landscape sweep, QC killed a phantom headline on machine facts (settings.json + one SSH); run 22 (voice in-session) found the prior run's own kill list stale (/voice had shipped) + the QC's doc fact both earlier agents missed flipped the winner - master re-verified it before accepting. |
| [implementation-relay-plan-build-review](implementation-relay-plan-build-review.md) | other | 1 | "Implement this batch of feedback on a system we own." 3 sequential agents: Research/Processing (map every item to real code + pull real values, plan file) -> Implementation (phased execution, checkpoint-to-disk discipline, one deploy) -> Review/Validation (independent audit, fresh renders, secrets grep, BLOCKER/MINOR/NIT, signed-off checklist). Orchestrator runs the brand pre-flight and pastes tokens into the implementer's prompt. Run 15 shipped HQ v2.1 (18 items) with it. |
| [document-tailor-build-review-qc](document-tailor-build-review-qc.md) | other | 1 | "Restructure my real document (CV/profile/proposal) for a specific audience brief, branded, nothing invented." 3 sequential agents: Build (draft + render + self-verify) -> domain-expert reviewer in the audience's seat (skim test, MUST-FIX/NICE, ATS text-layer check) -> QC final judgment (independent re-verification, traceability of every number, READY or fix list). Revisions relay back to Agent 1. Key lesson: reviewers get the VERBATIM source, never a compressed excerpt (false-positive risk). Run 18 shipped the Power BI finance contract CV. |
| [deck-rebuild-content-design-qc](deck-rebuild-content-design-qc.md) | other | 2 | "Rebuild this existing deck per my page-by-page feedback, same visual language, two formats." 3 sequential agents: content pass (per-page copy doc, exact diagram labels) -> UX designer (React HTML build, static render mode, self-reviewed renders) -> QC (word-for-word diff, brand scan, pptx from fresh renders). Master gates every handoff + views all renders. Key lesson: verify the brief's source mapping against the actual files FIRST; run 20's brief pointed at the wrong deck. **Reuse 2 (run 38, Alex deck v2 Brain/Spine/Hands): seats swap** - stage 1 becomes an ARCHITECT auditing live structured sources with a file pointer per fact (caught 3 wrong numbers), stage 3 becomes a WRITER doing story+copy LAST on the built deck, QC folds into the master's own render review. Build direct HTML, not Claude Design, whenever motion is the brief. Two slides silently dropped their footer and only per-page text extraction caught it. |
| [decision-brief-inventory-architect-debate](decision-brief-inventory-architect-debate.md) | decision-brief | 1 | "My own structure looks messy, change or keep?" 3 sequential agents: Cartographer (inventory, declared vs actual, flags only) -> Architect (exactly 3 philosophy-distinct options vs stated pain + Hcases) -> Debater (steelman/attack, eliminate to ONE + amendments, head-to-head vs the incumbent). Master reviews every hand-off vs the registry, owns the verdict. Key lesson: hand the debater the owner's REAL couplings (backup whitelist, gitignore, incident history) so eliminations land on verified facts. Run 21 (outputs/ layer) -> amended-Ledger verdict. |
| [design-review-dual-lens-debate](design-review-dual-lens-debate.md) | other | 1 | "Review this UI through multiple specialist lenses, debate it, give me a dev-ready plan." 2 INDEPENDENT reviewers (UX + UI) on the RENDERED live page -> master curates + source-verifies top claims between hand-offs -> adversarial debate (steelman/attack, kill/defer/merge, ranked change set + QA gates + sequencing waves) -> master verdict + plan. Key lesson: independence produced 4 convergent findings = the plan's highest-confidence tier; the honesty law must bind the debate or it ships tidy fabrications. Run 25 (Alex HQ) -> 21-item 3-wave plan. |
| [other-system-upgrade-audit-design-plan](other-system-upgrade-audit-design-plan.md) | other | 1 | "Upgrade my whole system: review everything, design it, give me a QC'd implementation plan." Master-gated relay: Agent 1 audit as N parallel Explore lanes (flags-only, lane-prefixed IDs) -> master corrects vs ground truth -> Agent 2 upgrade design (per-project verdicts, [OWNER DECIDES] flags, rejection list) -> master standing-order sweep (binding MR2 annotations; caught a privacy leak) -> Agent 3 code-verified QC (SHIP/FIX/REJECT + missed-flag sweep) + phased plan (verification/rollback/quality bar per phase) -> master synthesis with decision batch. Recommendation-only; 4 write-first working docs. Run 24 (full Alex upgrade) -> 14-phase plan, 0 rejects. |
| [technical-evaluation-sequential-plan-validation](technical-evaluation-sequential-plan-validation.md) | technical-evaluation | 1 | "Validate these PROPOSED implementation plans against our live system before we build." Master-gated adversarial relay: Agent 1 architect validates plans vs ground-truth FILES + vendor docs (FIT/FIT-WITH-CHANGES/MISFIT) -> master gate settles the agent's self-named weakness personally -> Agent 2 architect adversarially re-verifies + debates (HOLDS/FALLS/AMENDED, no manufactured dissent) -> master resolves (rebuttal only if genuinely contested) -> Agent 3 engineer QCs + writes ONE unified plan (real repo conventions, tests, rollback, UNRESOLVED ledger, owner decisions). Run 28 (three expansion plans) -> F6 killed by the deployed export, 22/31 lock drift proven live, unified 10-phase plan. |
| [other-strategy-ideation-debate-architecture](other-strategy-ideation-debate-architecture.md) | other | 1 | "Design a completely NEW strategy for a project of ours, debate it hard, hand me a buildable design." Master-gated: baseline brief (facts only) -> Agent 1 domain strategist CREATES (evidence-anchored, self-review required) -> master gate with mandatory challenges -> Agent 2 debates (HOLDS/FALLS/AMENDED) + adds own ideas + converges -> master gate -> Agent 3 AI architect (/new-ready pack, Phase 0 = zero blocked deps). Run 29 (modeling growth loop) -> ToS kill of the scraper radar, StarNow double-correction, honest-numbers rewrite. |
| [other-website-review-triple-lens-relay](other-website-review-triple-lens-relay.md) | other | 2 | "Review my live site through the buyer's eyes + a design lens, benchmark vs 5 verified peers, hand me a buildable plan." Master baseline first (own findings enable convergence detection) -> buyer-seat + UX lanes PARALLEL (independent) -> master gates each (spot-check + debate + conclusion) -> web-dev agent writes the wave-sequenced step plan vs the real stack -> master traces every step, assembles the full package. Run 31 (shaheenkiarash.com) -> 6/10 verdict, 30-step plan; write-first survived a mid-run quota wall. Reuse 2 (run 32, modeling marketing plan): seats swap freely (marketing assistant + IG content manager + budget-holder planner); binding gate-conclusions + trace tags + a NEW-items cap keep agent 3 honest. |
| [other-evidence-upgrade-pass](other-evidence-upgrade-pass.md) | other | 1 | "Sweep the live web (named sources) for the newest ideas to improve this FINISHED plan, debate vs the original, conclude v2." 2-3 PARALLEL source-domain scouts (each: v1 file + lanes + an explicit CHALLENGE lane + evidence-strength labels + top-10) -> master (holding v1 in context) debates personally, citations as the refutation anchor -> self-contained v2 with a FELL/AMENDED/CONFIRMED verdict table; v1 stays as audit trail. Run 30 (modeling v2) -> 4 FELL / 9 AMENDED / 7 CONFIRMED; fetch-blocked lanes rerouted + age-labeled; the pixels rule caught a silent 1-page PDF failure. |
| [decision-brief-dual-concept-merge](decision-brief-dual-concept-merge.md) | decision-brief | 1 | "Validate TWO external concept docs against our real system, then conclude ONE function that is their validated merge." Two PARALLEL per-document Senior-architect validators (each: web-check load-bearing claims + infra-map + gap confirm/refute by grep + reuse plan) -> master debates + source-verifies each -> merge architect writes the unified plan under a ship-independently/compose-optionally hard constraint -> master final gate (scope: a PLAN not a build; sequence components by conviction). Run 33 (Compounding Layer = Outcome Ledger + Reusable Workflows) -> BUILD, B-first. |
| [other-narrative-mine-debate-plan-qc](other-narrative-mine-debate-plan-qc.md) | other | 1 | "Mine this narrative doc ABOUT a system we own for real upgrades, debate, plan, QC." Master-gated sequential relay, single agent per stage, master debates TWICE: Agent 1 miner (extract + cull noise + live-ground every idea) -> master Debate 1 (redesign/merge/add/kill -> ranked shortlist) -> Agent 2 planner (technical plan vs the real files) -> master Debate 2 (reclassify/reject/harden -> final plan) -> Agent 3 BLIND QC (cold context, re-verify every claim against code). Trap: a doc that DESCRIBES an existing system reads like a wishlist; live-grounding is the discipline. Run 34 (Alex story-upgrade) -> 5 upgrades + 1 rejected, all "finish a find-out intention into a mechanism," blind QC PASS-WITH-FIXES. |
| [market-scan-pain-mine-casestudy-synthesize](market-scan-pain-mine-casestudy-synthesize.md) | market-scan | 1 | "What does [segment] actually struggle with, and where could [X] help?" The relay family's first pure EXTERNAL market scan. 3 sequential senior-researcher lanes, master gates each: Agent 1 demand+gap miner (hated-task mining + NEGATIVE tool reviews) -> master gate (kill fabricated pay-signals, patch a weak lane, down-weight vendor-marketing rows) -> Agent 2 case-study/what-people-automate (Indie Hackers founder stories least-biased; SATURATED vs STILL-OPEN through the non-technical-owner lens) -> master gate (convergence from INDEPENDENT evidence bases = highest-confidence tier) -> Agent 3 synthesizer builds the findings doc (Task table + confidence) from the working files. "Would they pay?" reads from revealed workaround spend, never invented. Run 35 (small-business automation pain scan) -> financial-admin the top-hated cluster, "too technical to set up myself" the gap; Reddit+Chrome gated (logged honestly, patched with US surveys); the pwd -W print-to-pdf gotcha caught by the pixels rule. |
| [other-academic-paper-evidence-lanes](other-academic-paper-evidence-lanes.md) | other | 1 | "Write me an academic paper (lit review + proposed study), citation style X, ~N words, as a document." 4 PARALLEL evidence lanes (domain evidence / theory spine / case context / instruments+method) under a binding evidence law: every citation needs a live hit + DOI/URL + retrieval date, incomplete bylines are EXCLUDED never repaired, a fabricated reference fails the run. Master frames RQs first, writes alone, builds the doc programmatically with deterministic checks (word band = fix the manuscript not the report; zero em-dashes; all refs cited; APA order). Run 41 (AI + EFL motivation, Iraqi universities) -> 62-ref APA 7 .docx, 3,734 words; 13 incomplete records honestly cut. |
| [decision-brief-teardown-replace-relay](decision-brief-teardown-replace-relay.md) | decision-brief | 1 | "Wipe this existing project whole and replace it from an external spec — give me the safe plan." Pre-relay Explore demolition survey (risk-tiered map, saved as a deliverable) -> Agent 1 infra reviewer VERIFIES the survey + per-file dispositions -> master debate -> Agent 2 argues the spec's adoption + amendments (hardest call argued both ways; frozen-scope changes flagged for the owner, never silently ruled) -> master debate -> Agent 3 adversarial QC + ordered implementation plan with exact registry mechanics -> master final md with claims table. Run 40 (#30 modeling -> portfolio-site): validator couplings ARE the plan's skeleton (V2 disable-then-delete; C3/C4 tombstone mechanics); the target's own next scheduled fire sets the clock. |
| [technical-evaluation-aspect-fan-reconcile](technical-evaluation-aspect-fan-reconcile.md) | technical-evaluation | 1 | "Assess this external system aspect by aspect vs my setup, then reconcile with the prior plan into ONE plan of record." Master prep (deterministic tree scope, delegated roster + persona casting, dossier) -> parallel cold-context aspect lanes (fixed deliverable shape, own-ideas mandate, dossier re-derivation duty) -> master validation per lane with personal re-fetch of decisive claims -> synthesis (convergence tiers, 5-year test, negative imports) -> reconciliation table VALID/AMENDED/INVALIDATED -> merged plan + plain-English twin. Run 47 (ECC): the counting law (summarizers quote, never count), run-state file + in-place resume at the usage wall, hardened rail for installable targets. |

| [decision-brief-diagnose-solve-rank](decision-brief-diagnose-solve-rank.md) | decision-brief | 1 | "Diagnose this systemic problem in my own system, design N solutions, stress + rank them." Owner-dictated 3-agent relay, master corrects between EVERY handoff via written scratchpad artifacts (corrections block + full corrected text, F/C/R ids). Measurement replaces citation (every number carries its command; master re-derives). Run 44 (file bloat): all three review passes overturned something real - a 40% token-ratio error, a 2.4x disk undercount, a definition self-reference, and the stress agent's arithmetic kill of a solution's steady-state claim. |
| [other-medical-document-translate-qualify-debate](other-medical-document-translate-qualify-debate.md) | other | 2 | "Read this foreign-language medical document and tell my family what it means." Fixed document, no external research, lay audience in a second language, high harm from a confident wrong answer. 3 SEQUENTIAL master-gated agents: translator+validator (independent flag per row, absences verified by search) -> senior physician (severity, together-not-separately, ranked differential, management) -> adversarial (rules on the master's BINDING challenges, audits point by point, hunts what both missed, resolves into the lay-language final). Master extracts the source ITSELF before agent 1 so the agent becomes a second independent read, recomputes every arithmetic claim, and hands the adversary a specific asymmetry rather than "go attack this". Retro-fitted after run 43; run 23 was the unwritten first. Key lessons: a printed lab flag is not automatically a patient finding (an IMG# "L" was a range-config defect provable from inside the document); the adversarial seat pays off in the DIFFERENTIAL (run 23 added celiac, run 43 caught an alpha-thal diagnosis prescribed a beta-thal test); a conditional does not survive translation; RTL means wrap in logical order and shape per line at draw time. |

Rules:
- A pattern earns its file by being used, not imagined. No speculative patterns.
- Update `last_used` and `times_used` on every reuse; append to Lessons when something changed.
- If a pattern fails twice, rewrite it or delete it.
