# Recovery Layer (18)

## Type
Automation (local, zero-token deterministic checker). Scheduled weekly + on-demand.

## Purpose
Alex's third layer (Import → Update → **Recovery**). Proves every change actually propagated, and
keeps the system verifiably healthy. The [[research/alex-recovery-layer|Change Propagation standing
order]] is edge-triggered: it fires per change and, if a session dies mid-propagation (context loss,
quota), the miss is permanent and invisible. This is the **level-triggered** layer (Kubernetes/
Terraform style): a scheduled sweep re-checks the WHOLE system against a desired-state manifest,
forgiving of missed events. The **checker (`check.mjs`) detects, never auto-repairs** (IaC warning);
judgment stays with Shaheen and the monthly LLM /lint. Design + evidence: [[research/alex-recovery-layer]] (pieces 1-2).

**FIX half added 2026-07-21 - the HQ Self-Heal Loop** (`scripts/hq_self_heal.py` + registry `system/hq-heal-map.json`).
The checker stays detect-only, but HQ-metric drift now gets an automatic scoped repair on **every HQ update**
(folded into the harvest): AUTO-SAFE mismatches (MCP count, stale box JSONs, drifted metric) are re-derived +
re-pushed + read-back-verified; PROPOSE (live workflow mutation, stuck-flag clear) + HUMAN-ONLY (phone/OAuth)
are queued to human-actions with a diagnosis, never auto-run (Shaheen's autonomy boundary 2026-07-21); a
catch-all flags any unclaimed red. So recovery now DETECTS across the whole system (checker) AND FIXES the safe
HQ class automatically (heal loop). Full spec: root CLAUDE.md "HQ Self-Heal Loop" + [[projects/recovery/status]].
The IaC "detect-only" warning still governs `check.mjs`; the heal loop is the deliberate, bounded exception.

## Entry Points
- **Scheduled:** `PersonalOS-recovery-check`, Monday 07:30 (shares the Alex Radar sweep slot). Pure Node, no `claude` call, zero tokens.
- **On-demand:** `node work/18-recovery-layer/check.mjs` any time.
- **Baseline:** `check.mjs --init` after real structural changes (records CLAUDE.md hashes + log high-water + the C28 user-scope skill inventory into state/).
- **Test:** `check.mjs --dry-run` runs the sweep and writes the report but does NOT push to Alex HQ.
- **n8n active-flag watcher (sibling, daily):** `PersonalOS-n8n-active-check`, daily 08:10 -> `scripts/n8n-active-check.mjs` (also zero-token, `--dry-run` to skip the HQ push). Reads the manifest's LIVE projects that map to an n8n workflow id (#3/#12/#14/#15/#16/#17/#31/#32), GETs each, asserts `active==true`, and pushes RED `recovery/n8n_active` + exit 1 if any expected-active workflow is OFF. A total-API outage is amber + exit 0 (transient, never a false RED). **Why it exists (BUG-01, 2026-07-16 diagnostic audit):** n8n's activate/deactivate does NOT bump `updatedAt`, so a silently deactivated LIVE workflow (the 2026-07-10 dual-engine class) is invisible to any timestamp check - you must read the flag itself, and the weekly sweep is too infrequent to catch a same-day miss. The still-open class fix is a shared PUT-with-active-hard-restore helper so no ad-hoc n8n REST write can drop the flag in the first place. **LEG 2 added 2026-08-05 (pen-test P-02): execution health.** The flag leg answers "is it switched on"; it does not answer "did it work", and those are different invariants. Proven 2026-08-04/05, when #03 and #14 both ERRORED on the live box while this watcher, validator V6 and the weekly sweep all read green - every one of them inspects CONFIGURATION, so three greens on the same layer were not independent evidence. Leg 2 makes ONE `/executions?limit=250` read and asserts per governed workflow: **(a)** the last execution did not `error`/`crash` -> RED immediately; **(b)** a SUCCESS exists inside the window its declared `n8n_cron` implies (daily -> 2d, weekly-ish -> 8d, both doubled for slack) -> catches the silent case where a trigger stopped firing at all, which (a) structurally cannot see because there is no failed execution to find. Only lanes that DECLARE an `n8n_cron` get leg (b); #16 and #17 are webhook-driven with no registry cadence, so asserting one would invent a contract that does not exist (their silence stays owned by the HQ self-heal `health-source-stalled` probe). Executions unreadable = leg 2 skipped, never a false RED, same posture as the flag leg's API-outage case. Still DETECT-only: no rerun, no auto-fix, because the causes are not interchangeable - 2026-08-04 alone produced a Sheets 503, a Bright Data "Customer is not active" and an unparseable Sheets range, three different remedies and none of them a retry. Green now reads "active + last runs healthy", deliberately, because the old "all N active" headline is exactly the reassurance that hid P-02.

## Tools Used
Node (node:fs / node:crypto / node:https + `systemctl --user` for the scheduler half), the n8n-free local filesystem, Alex HQ push webhook. No LLM, no MCP, no network except the one HQ push.

## Infrastructure (as built 2026-07-04)
- **Manifest** `system/manifest.json` at the repo root (moved out of this folder 2026-07-08, refactor amendment A2: a system-wide registry must not live inside one numbered project) - since 2026-07-06 (audit steps 3+5) **THE project registry**, not just the checker's input. Per project: num, name, title, state (LIVE/ON-DEMAND/EVENT/DORMANT/PARKED/RETIRED + revisit dates), trigger, one_liner, docs, schedule_jobs[], hq_project, work_dir, commands[], status_md, `cadence` object (**upgrade P4, 2026-07-12: replaced the old cadence_days integer** - `{expected_hours: N|null, label, note?}`, the staleness window + render class HQ and the checks age against), `first_fire`/`first_fire_kind` (the date a project first produced for real; null = never fired, kind live|drill), n8n id + `meta.unnumbered` (voice, alex-costs, modeling) + the allowlists. **`hq_project` is also consumed (2026-07-07):** it's THE project→Alex-HQ metric-slug map that `work/16-alex-hq/scripts/build-projects.mjs` reads to render the HQ Automation Health board FROM this registry (so every registered project shows, not just producers); set it to the exact slug a producing project pushes under, null if it pushes none. Edited BY HAND when anything project-level changes; then run **`node scripts/generate-alex.js`** (the unified generator, since 2026-07-08; it absorbed the retired generate-surfaces.ps1), which regenerates the root CLAUDE.md routing table AND the docs/projects/README table between markers (never hand-edit those) plus the other generated docs. /new writes its registry entry first. This is the source of truth the sweep validates disk against AND the source the human-facing tables are generated from - one edit, no propagation drift.
- **Checker** `check.mjs` - the sweep. Exit **0 clean / 2 drift / 1 checker-error** (Terraform `-detailed-exitcode` convention). Writes `vault/projects/recovery/last-sweep.md` (human report the Monday brief reads) and pushes `recovery/integrity` to Alex HQ (green clean / amber drift, value_num = distinct-finding count). Log: `outputs/logs/recovery-check.log`. **Hardened 2026-07-04 (QA review [[research/recovery-layer-qa-review]]):** repo root derived from the script's own location (survives a restore to any path/machine); the whole sweep is wrapped in a **fail-loud** try/catch that pushes RED integrity (value_num -1) + exit 1 on a checker error, so it can never sit stale-green while dead; the HQ push is best-effort (a bad token/network never fails the sweep).
- **State** `state/baseline.json` (CLAUDE.md hashes + last_init from `--init`) + `state/log-highwater.json` (monotonic log line count) + `state/user-skills-baseline.json` (the C28 accepted user-scope skill inventory, written by `--init`). Gitignored (local runtime state).
- **Public-repo CI** `.github/workflows/ci.yml` (2026-08-05, enterprise-assessment idea 2, [[research/enterprise-assessment-ideas]]): GitHub Actions on every push to main + every PR runs the PORTABLE tests - the ones proven to pass on the tracked vault-less tree alone (test-lesson-parse, test-protected-guard, test_vault_search_freshness, soul-canary.test.mjs with its clone-aware live-soul skip, close-out.test.mjs, test-untrusted-guard added the same day with idea 5). No secrets, `contents: read` only. This is the repo-side, per-push complement of the Monday sweep: it catches test regressions in DAYS-old commits instead of at the next weekly slot, and it graded itself immediately - building it found the completion-sentinel test broken-and-silent since the 07-21 Stage-2 sentinel flip (the enforcing path's `exit 1` killed the test process before one assertion printed; fixed same session - the Node port's check function returns its code instead of exiting, so the class is structurally gone). The full validator stays local-only BY DESIGN (it asserts the live scheduler, live n8n and gitignored identity files); its testable core rides in via test-protected-guard. First run: GREEN in 24s, run 31043243311 (windows-latest at build; retargeted with the platform in the 2026-08-25 reconciliation).
- **Machine-rebuild bootstrap** `scripts/bootstrap.mjs` + `system/environment-schema.json` (2026-08-05, enterprise-assessment idea 6, [[research/enterprise-assessment-ideas]]; ported from bootstrap.ps1 in the 2026-08-25 reconciliation): the environment doctor for a fresh machine. The schema (tracked, deliberately NO secret paths - S9 forbids them in tracked files) declares every outside-repo dependency: tools with restore commands + off-PATH fallback paths (gpg's fallback list is probed exactly as the vault backup resolves it), npm globals, python-docx, ssh alias `n8n`, the four `~/.claude/settings.json` items from identity §4, git remote + core.longpaths (asserted on Windows only, where it matters). Rules that would drift if restated POINT at their source instead: scheduler jobs come from manifest `schedule_jobs` (recreation stays /cron-setup's; the doctor probes systemd first, Task Scheduler on Windows, and reports honestly when neither is reachable), secret-file existence comes from the gitignored `credentials-ledger.json` (checked present, values never read; ledger absent = "restore the encrypted tar FIRST"). DOCTOR, NOT INSTALLER: reports PASS/MISS/OPT + exit 0/2/1, and repairs exactly one thing, `--repair-links` (Node `fs.symlinkSync(..., 'junction')` - no elevation on Windows, a plain symlink on POSIX, one code path for checkout-dropped skill links). First live run found 2 real items on THIS machine: the gpg probe gap (schema now mirrors production's fallback order) and `core.longpaths` unset despite the restore doc requiring it (set repo-local same session). Log: outputs/logs/bootstrap-check.log.

## The checks (29 total - C1-C30 with C16 retired, all deterministic, grow from real misses per the design)
1. **quad completeness** - each manifest project has its work dir, work CLAUDE.md, status.md, and every declared command file.
2. **orphan commands** - every `.claude/commands/*.md` is owned by a project or the utility allowlist (caught `/venture-sync`).
3. **orphan work folders** - every `work/` dir (not just `NN-*`) is a manifest project or in the `meta.known_work_folders` allowlist (`voice`, the on-demand voice loop); catches a rogue non-numbered folder.
4. **orphan vault projects** - every `vault/projects/*` is registered or a known extra (caught `modeling` + stale `market-pulse`/`opportunity-scout`).
5. **wiki-link resolution** - every `[[link]]` resolves. `vault/archive/` + `vault/sources/` are valid *targets* (real files); excluded as *sources* are index.md, log.md, the checker's own last-sweep.md, archive/, sources/, and immutable dated records (`history/`, `standups/`). **Path-style links** (`[[a/b]]`) require the full relpath OR a *unique* basename (so `[[people/name]]` resolves per the People Protocol, but `[[x/status]]` does NOT resolve via any `status.md`); bare `[[name]]` uses basename. Code-span links (examples) are stripped; cross-tree links to real `work/`/`sources/`/`outputs/` files resolve. Report ranks top distinct unresolved targets by count.
6. **routing rows** - a real `| NN |` routing-table row carries the project's work_dir (not just any prose mention; zero-pad tolerant).
7. **scheduler ↔ systemd timers** - every documented `PersonalOS-*` job is registered and vice versa. Ephemeral `PersonalOS-retry-*` one-shots (the close-out lib's self-scheduled retries, 2026-07-06) are excluded - they come and go by design. **C7b TRIGGER TIMES (added 2026-07-25, stress-test F-05):** the documented hour must also equal the LIVE hour. Until this, EVERY scheduler check compared names only (C7 doc↔live, V2's live half doc↔live) or two docs to each other (C16 label↔Frequency text), so a job whose trigger TIME was hand-edited or mangled by a task re-creation fired at the wrong hour forever while every surface read green. The expected time is parsed from the section's `- Frequency:` line ONLY (never body prose, which legitimately names other times - the Application Engine section documents the n8n engine's 15:00 beside the local watch's 08:30); a Frequency line with no parseable clock, an on-demand/event entry, or a trigger with no StartBoundary is SKIPPED, and a task with several triggers passes if ANY matches. Category `scheduler-time`. Negative-proven at build against a throwaway task (retime → flagged with both times, correct the doc → clears, zero residue).
8. **dependent staleness (hash-based)** - flags a project whose work CLAUDE.md hash changed since the last `--init` but whose status.md hash did NOT (a real propagation gap). Mtime-immune, so a mass write (privacy scrub) or a git clone can't false-positive or mask it. Resolution: propagate for real, then re-run `--init`.
9. **log monotonicity** - `vault/log.md` line count must never drop (append-only; guards data loss).
10. **uncommitted-spec drift (git-HEAD based, CHANGED 2026-07-21 audit F-02/Class B)** - a work CLAUDE.md that differs from its COMMITTED version (`git diff --quiet HEAD`) is flagged. Was "changed since the last `--init`", which flagged every committed edit until a human re-ran `--init` (13 stale rows at the audit, chronic noise that masked real drift). Now COMMITTED = ACCEPTED: only an UNCOMMITTED / out-of-band edit flags, which is stateless (git IS the baseline, so C10 can never itself go stale). The "re-review the manifest one-liner when a spec changes" nudge moved to the weekly `/self-review` (it diffs `work/**/CLAUDE.md`). Falls back to the `--init` baseline only when git is unavailable (pre-first-commit restore). C8 KEEPS the `--init` baseline on purpose - it catches a COMMITTED spec change whose status.md was not propagated, which git HEAD cannot see.
11. **index ↔ disk** - each manifest project's status page is catalogued in `vault/index.md` (a registered project missing from the catalog is caught; the design's named piece-2 check).
12. **outputs naming (2026-07-11, the amended-Ledger build [[research/output-structure-review]])** - outputs/ top-level dirs must be manifest keys or the declared exemptions; calls `node scripts/outputs-ledger.js validate` (exit 2 = drift) so the exemption list has ONE home. Detect-only here; healing = the nightly reconcile in vault-backup.sh. Guards the backup whitelist against silent folder-name drift (the interview triple-name class).
13. **first-fire aging (C13, upgrade P4, 2026-07-12)** - a LIVE/EVENT registry row (numbered + unnumbered) with `first_fire: null` may age at most 14 days from its status.md frontmatter `created:` date (manifest states_doc rule). Past that = amber until it fires (a documented drill counts, `first_fire_kind: drill`) or is re-stated with a reason. ON-DEMAND/DORMANT/PARKED/RETIRED exempt. The generator's V9 warns on the same condition; also ambers a null-first_fire LIVE/EVENT row whose status.md has no `created:` date to age against.
16. **cadence-vs-schedule (C16) - RETIRED 2026-07-25** (Shaheen: "APPLY"). Deleted, not repaired: its job is now done STRICTLY BETTER by C7b (every live timer's trigger hour vs the hour schedule.md documents) and validator V6 leg (c) (each project's declared `n8n_cron` vs the live scheduleTrigger) - both doc-vs-REALITY instead of doc-vs-doc. It is deliberately NOT written as a `// --- C16` header in check.mjs, because that is the pattern narrative-drift-check.py counts, and a retired check must not inflate the live total. The NUMBER 16 is retired and never reused, so every dated reference to C16 stays meaningful. Historical description follows. Manifest `cadence.label` vs the `- Frequency:` text in scheduler/schedule.md, per project carrying schedule_jobs (deterministic label->pattern map: daily/weekdays/weekly/monthly/always-on/**twice weekly (Tue & Thu)**; a project passes when ANY of its schedule.md entries matches). Labels with no frequency expectation (expected_hours null) are skipped. **Two 2026-07-25 corrections (stress-test F-06):** (a) the `twice weekly (Tue & Thu)` label the job engines have carried since the 07-24 retime was MISSING from the pattern map, so C16 silently skipped both engines (an unmapped label reads as "no expectation"); (b) with the label mapped, #03 immediately exposed a real conflation - its cadence describes the REMOTE engine (Tue & Thu 15:00 on the box) while its Windows job is a deliberately DAILY 08:30 local watch, so comparing them reports permanent false drift. Fixed in the DATA, not the check: a cadence block may now declare `"scope": "n8n-engine"`, which makes C16 skip the label compare and hands that fact to validator **V6 leg (c)** (manifest `n8n_cron` vs the live scheduleTrigger, doc-vs-REALITY, strictly stronger than doc-vs-doc). Projects whose cadence describes their local jobs (the default, e.g. #15's Monday 07:30) keep this check. 14. **passphrase attestation (C14, upgrade P10, 2026-07-12, closes audit c14)** - `state/passphrase-attested.txt` carries a yyyy-MM-dd first line. **Single-source-of-truth since 2026-07-21 (audit F-01/Class A):** the attestation file is written ONLY by the escrow drill (`escrow-test.mjs`), which on a PASS stamps today's date AND closes the escrow queue items in the same run, and on a FAIL writes PENDING + keeps the queue item open. `scripts/human-actions.js done` REFUSES to close `passphrase-attestation`/`passphrase-escrow-retest`/`passphrase-safeplace-fix` unless this file is a real dated attestation - so the 2026-07-18 contradiction (queue said done while the drill had FAILED) is now unrepresentable. Missing file, malformed date, or >90 days old = amber (the 90-day re-check doubles as the rotation-review prompt, the c15 fold). The check never reads the passphrase itself. All three branches synthetic-proven at build; the PS 5.1 TryParseExact overload trap (explicit culture/styles required) was caught live by the fail-loud wrapper and fixed.
15. **PAT expiry window (C15, upgrade P10, 2026-07-12, closes audit c17)** - the GitHub backup PAT expires ~2027-07-01 (`$patExpiry` constant in check.mjs, updated on rotation); amber inside 60 days, louder past expiry. The credential itself is never read.
17. **skills-symlink restore guard (C17, deep-audit BUG-16 fix, 2026-07-15)** - every `.agents/skills/<name>` (committed content) must have a resolving `.claude/skills/<name>` link. The link layer is gitignored (POSIX symlinks), so a restore that skips the rebuild leaves MANDATORY skills silently unloadable. Amber lists the missing pairs; rebuild with `ln -s`.
18. **machine timezone vs travel-state (C18, P8 scheduler timezone audit, 2026-07-17)** - the local systemd timers fire at the machine's wall clock (see the Timezone Policy in scheduler/schedule.md). Expectation = the home tz ("Europe/Stockholm", IANA since the 2026-08-05 Linux move), UNLESS `system/travel-state.json` (P7 trip-ops 1b) declares an active trip with a `current_tz`. Amber on a mismatch so follows-Shaheen / must-anchor jobs never silently fire at the wrong hour. Detect-only; handles a missing/invalid travel-state gracefully. (The plan reserved "C18" for the P2 chat-poller check; P8 shipped first, so the P2 check takes the next free number.)
19. **narrative numbers-drift (C19, item 3, 2026-07-20)** - the identity-carrying master reference must not claim a recovery-check count the code disproves. Shells to `scripts/narrative-drift-check.py` (zero-token, ONE home for the claim-set, the C12 shell-out pattern), which counts check.mjs's `# --- C<n>` block headers for the live count and asserts the master ref's canonical "**N deterministic checks**" phrase matches. Detect-only. Born from the 2026-07-20 story-upgrade run: the same-session-update standing order for the narrative docs was the last place Alex trusted a habit over a mechanism, and the master ref had already drifted (said 17, code ran 18). MVP scope = the master reference .md; the plain-English guide .docx (python-docx table extraction) is phase 2. The bare `C1-C<n>` range form is deliberately NOT enforced (it appears in dated running-changes entries that legitimately carry the count-at-that-date). **REPOINTED + EXPANDED 2026-07-25 (stress-test F2):** the master moved OUT of the repo 2026-07-21 but this check still pointed at the old in-repo path (now a 481-byte retirement stub), so it read the stub, found no claims, and passed vacuously - the living doc was guarded by nothing while its claims drifted. Now the master path is read from manifest `meta.paths.master_reference_md` (SSOT, `%USERPROFILE%`-anchored so a restore to another user doesn't perma-fail); an ABSENT master AMBERS (never silent-green, the anti-re-rot lever); and the claim-set is DERIVED from ground truth: the check-count (check.mjs), the V-count (validate-alex.js), the non-retired project count (manifest), and the escrow date (state/passphrase-attested.txt). Claims are read only from the STANDING doc (before "## 11. Running changes") so dated history is never false-flagged. Each new claim needs its own regex+source pair; pure prose drift stays the /lint's job.
20. **backup destinations (C20, stress-test F1, 2026-07-25; message honesty F-14 same day)** - `>=2` INDEPENDENT off-machine backup destinations must each verify a copy this cycle. **F-14 correction:** the amber now distinguishes "never stamped" from "failed". On 07-25 C20 read "hetzner-n8n (no verified copy in 72h)" while the truth was that F1 stamping shipped that morning and the first stamped run had not happened yet - the primary backup log was fine. An UNPROVEN destination (the `state/backup-destinations.json` file does not exist at all) now says so in those words; reporting a healthy-but-unstamped leg in the same language as a rotted one is how an amber teaches people to ignore it. Kills the SPOF where the sole off-machine home was the PRODUCTION n8n box (a box+laptop double loss was unrecoverable). Declared destinations live in manifest `meta.paths.backup_destinations` (Hetzner scp + Backblaze B2 via rclone); `vault-backup.sh` stamps `state/backup-destinations.json` per verified ship; C20 ambers (never reds) until `>=2` are fresh within 72h, so a silently-rotted 2nd leg can't hide behind a green primary and the B2 provisioning (human-actions f1-b2-backup) stays visible. Detect-only.
21. **facts-ledger doc drift (C21, Recall Spine Phase 1, 2026-07-25)** - standing IN-REPO docs tested against the bi-temporal fact ledger `system/recall/facts.db` (the Recall Spine). Shells to `scripts/facts-check.js` (exit 2 = drift, the C12/C19 shell-out pattern), which self-harvests then diffs each mapped doc claim against the fact the ledger derived from a STRUCTURED source (manifest, validate-alex.js, check.mjs's own `// --- C<n>` headers, the live timers, skills-lock, the attestation file). **DIRECTION: the doc is the test subject, facts.db is the expectation** - so it does NOT reintroduce the V6 anti-pattern (deriving an expectation FROM prose). Pays the ST-20/FR-04 "a doc lying about the system" debt for the machine-checkable claim class (it caught its own "20 total" line the moment C21 was added, proving the loop). Seed map: this doc's check-count header + the root CLAUDE.md model-routing default; the map grows one `{doc-regex + fact}` row at a time. **Complements C19** (narrative-drift, the OUT-OF-REPO master doc, F2-repointed 2026-07-25): C21 owns the in-repo prose surface, C19 the out-of-repo master, no overlap. Detect-only.
    **WIDENED 2026-07-29 (architecture review, the CLASS fix) - C21 now also sweeps every `work/NN/CLAUDE.md`.** That review found ELEVEN spec-vs-registry disagreements at once, and the reason they had accumulated silently was that the project specs were the one surface with NO checker: C1 asserts a spec file exists, C10 notices it changed, V7 reads schedule prose, but nothing had ever read what a spec CLAIMS about itself. The sweep is GENERIC, not 32 hand-written rows (a per-project row set would drift exactly like the prose it polices, and a new project would arrive uncovered). Two assertions only, both decidable from `system/manifest.json` with no judgement: **(a)** a spec must not name a model provider that `meta.model_routing` does not carry (the #23 class: that spec advertised "OpenAI" for three weeks after the 07-08 rule change while the contract named none and the wrapper pinned sonnet); **(b)** a spec must not deny a schedule the registry gives it (the #13 class: "No separate schedule" against a live monthly job). **Lifecycle-state prose was evaluated and deliberately REJECTED for v1** - specs name states in history lines, comparisons and superseded notes, so any regex broad enough to catch real drift also fires on correct prose, and a checker with false positives gets ignored, which is worse than no checker. Tuned against all 32 live specs to zero false positives, and **negative-tested**: re-introducing the real #23 and #13 drift makes it fail, and both files were restored byte-identical after. The biggest false-positive source turned out to be the repo's OWN inline supersession convention (a correction quotes the value it replaces), so `stripCorrections()` removes those spans before scanning and the checker reads only the live claim.
22. **soul-corpus monotonicity (C22, command-layer review F-1, 2026-07-28)** - the `soul.md` "My Words" corpus must never SHRINK: C9's log-monotonicity shape applied to the highest-value file in the repo (high-water in `state/soul-highwater.json`, counts dated `### Harvested YYYY-MM-DD` / `### YYYY-MM-DD` entries plus raw line count, flags either dropping; a MISSING soul.md flags outright). **Why it exists:** `/setup` step 4B said "OVERWRITE the template ... Under 2.5KB" with no fresh-install branch, so any agent running `/setup` on a live install would truncate a 115KB corpus built over months from voice transcripts and the typed-capture hook. The failure is SILENT - every prose surface (n8n writer nodes, CVs, cover letters, LinkedIn, email drafts) keeps working, it just stops sounding like Shaheen - and **Close-Out V cannot catch it**, because V only asks whether My Words gained AN entry today, which a freshly overwritten file satisfies perfectly. soul.md is gitignored, so no git-based guard (V10, V11, C10) can ever see it; the only copy is the 21:45 encrypted vault backup, last 14 kept, so a weekly sweep detects comfortably inside the restore window. `setup.md` also gained a STOP CONDITION at the top and a re-check at step 4B, but those are prose guards on an agent that reads carefully, which is not the class that causes this; C22 is the half that does not depend on anyone reading. Detect-only.
23. **soul-core freshness (C23, S1 Compiled Surfaces, 2026-08-16)** - `soul-core.md` (the compiled identity injection card) must exist and its tail `SOUL-CORE-STAMP` `source-sha256` must equal `sha256(soul.md bytes)` recomputed live (the same byte primitive the builder stamps with). **Why it exists:** harness 2.1.220 persists hook stdout over ~10KB to a file + 2KB preview, so the old `cat soul.md` SessionStart injection had been delivering ~2.3KB of a 172KB identity file to every session (measured from live transcripts 2026-08-16; it retroactively explains the 08-05 canary flake). The card now rides a `CLAUDE.md` `@soul-core.md` memory import (loads whole, proven at 36KB+) and is rebuilt nightly at 21:35 inside `run-vault-index.sh` + by every generator run, both under the shared write-lock. A STALE card = every session fed yesterday's identity slice; a MISSING card = sessions on the truncated full-soul fallback (fail-open by design, but not a state to sit in) - both AMBER. The FIX half is the hq-heal-map `soul-core-stale` AUTO-SAFE row: rebuild `--force` + read-back verify, one attempt then escalate. Builder: `scripts/lib/build-soul-core.js` (operative layer + BOTH canaries verbatim + pinned registers from `system/soul-pins.json` + ~20 newest entries by PARSED heading dates, refuse-below-floor, `git check-ignore` privacy fail-closed, atomic staging swap, unchanged-source = verified no-op). Negative-tested at install: doctored stamp -> C23 ambered -> heal probe rebuilt + verified -> C23 clean; 6 builder floor refusals + both hook branches transcript-proven. Detect-only here; the heal row owns the fix.

24. **status byte budget (C24, S1 Compiled Surfaces P2, 2026-08-16)** - every manifest-declared `status.md` must sit within `meta.vault.status_byte_budget` (25,600 B; fires at +10% so the keep-the-newest-dated-block rule's few-hundred-byte overshoot never trains amber-blindness, the F-14 lesson). Tier-1 pages are SUMMARIES by contract and had grown to 87-180KB; `scripts/status-rotate.js` (nightly, FIRST in the 21:35 chain so the index build sees the moves) moves whole dated H2 blocks - heading-block-atomic, so an inline supersession can never separate from the fact it corrects - oldest-first to `vault/projects/{name}/history/status-YYYY.md`, linked both ways, journaled to `system/status-rotate-journal.jsonl` BEFORE each move, verify-after-write from disk, shared write-lock, keep-the-newest-dated-block. C24 reads LIVE byte counts so a dead rotator cannot hide behind a green chain, and its message names the remedy class: "movable dated blocks present = the rotator missed" vs "undated standing weight = needs a human restructure (/lint-class)". First rotation 2026-08-16: 99 blocks across 8 files (morning-brief 87->24KB, email-triage 158->27KB, alex-hq 49->24KB, ai-job-pipeline, recovery, research-team, job-pipeline, prompting - the last three keep undated weight over budget, a standing /lint carry-over); BM25 retrieval of a moved block from its history/ home verified post-reindex. Negative-tested with a temp-inflated file (fired with the rotator-missed diagnosis, cleared on restore). Detect-only.

25. **inbound mail channels (C25, 2026-08-23)** - every custom address on the zone that forwards into Gmail (registry: `system/mail-channels.json`) is asserted to still have an ENABLED Cloudflare routing rule pointing at the verified destination, AND to have received mail inside its declared window. Shells out C12-style to `scripts/mail-channel-check.js` so this file's "no network except the one HQ push" contract holds. **Born from a real 2.5-month silent outage:** `shaheen@shaheenkiarash.com`, the ONLY contact address on the live portfolio site, stopped delivering around 2026-06-08 and nothing went red, because nothing was broken in a way any component could see. What a MISSING rule does depends on the catch-all: enabled+drop means the mail is accepted then binned and nobody is told; disabled means it is REJECTED at SMTP and the SENDER gets a bounce. **CORRECTED 2026-08-23 from a live API read: this zone's catch-all is DISABLED**, so an unknown address bounces rather than vanishing, and the first version of this entry claimed the opposite. Its cited evidence was also misread: two probe mails produced no bounce because they were DELIVERED and Gmail deduplicated the copies of Shaheen's own message, not because they were discarded. The gap C25 closes is unchanged, because in BOTH cases Shaheen hears nothing and he is the only observer the system can act for. **Two legs on purpose:** the config leg is the only thing that can watch a near-zero-traffic address (shaheen@ took 3 messages in 3 months, so silence there carries no signal and an arrival-heartbeat would never fire), and the outcome leg is the only thing that proves delivery rather than configuration. The daily half runs in the morning brief (Gmail is MCP-only) and writes `state/mail-channels-seen.json`; C25 reads that stamp, and a stamp older than 48h is itself drift because it means the daily leg died - this bug one level up. A channel with `max_silence_days: null` is deliberately NOT asserted rather than given an invented window.
**The Cloudflare MCP does NOT retire this check's API token (2026-08-23).** An official Cloudflare MCP server was added the same day at user scope, and it can read and write Email Routing interactively. C25 cannot use it: it runs headless from `check.mjs` on Monday with no MCP session, so it still reads the scoped token at `config/cloudflare-api-token.txt`, and `cf-token-swap-account-perms` stays a REAL human action rather than being closed as "the MCP covers it". Concretely, the destination-verification assertion is answerable in an interactive session today and still UNPROVEN on Monday, and the checker reports that in different words from a failure on purpose (the C20 / F-14 rule). Two MCP limits worth knowing before reaching for it here: adding the server mid-session does not register its tools (full restart required), and there is no per-message Email Routing log endpoint in the REST API at all, so a single dropped message is still not something either leg can catch. That synthetic-canary gap is open and known.
**`alex@shaheenkiarash.com` joined the watched set 2026-08-23** (`config_probe: true`), its rule created through the MCP and independently re-verified by this checker through the scoped token. Its `max_silence_days` stays `null`: the address is unpublished and Gmail filter B is designed to delete everything not forwarded by Shaheen, so silence is the intended state and a heartbeat would assert a contract that does not exist.

26. **vault/log.md tail ordering (C26, run-47 merged plan P1.5, 2026-08-23)** - the activity log is described everywhere as append-only and time-ordered and was measured on 2026-08-23 to be neither: 276 of 1,107 adjacent heading pairs ran BACKWARDS, one entry was stamped in the future, the file carried a UTF-8 BOM, and no script owned it (run-46 finding N3 - every other substrate that matters has a writer, this one was model-written prose at both ends). `scripts/log-append.js` is now the mechanical writer: appends at the TAIL only, stamps one clock, and REFUSES an entry older than the current tail unless forced. C26 is the level-triggered backstop for anything written around it: it asserts monotonic ordering and flags future-stamped entries, from `meta.vault.log_order_baseline` forward only. **History is BASELINED, not repaired** - the 276 pre-existing inversions record what actually happened and rewriting them to satisfy a checker would be a lie. AMBER, never RED (an ordering wobble is hygiene, not data loss). Negative-tested at install by lowering the baseline (fired 2 items) and restoring. Its first real run found a true positive: a `23:40` entry sitting physically between the 01:40 and 03:10 entries whose own body cited 2026-08-22 22:48 as already past - a clock error, stamp corrected inline with the original value recorded in the heading.
27. **soul-core byte budget (C27, run-47 merged plan P1.6, 2026-08-23)** - `soul-core.md` must sit within `meta.vault.soul_core_byte_budget`. **Why it exists:** the builder had a 60KB threshold that WARNED and shipped the oversized card anyway, which is the same dead-check-green shape as the backup's identity warning, and the card had doubled from ~36KB to 88KB in a week because the recency slice is entry-COUNTED, not byte-counted (run-46 finding N7 - the S1 diet eroding on an axis no check watched). `scripts/lib/build-soul-core.js` now TRIMS to the budget, dropping the oldest entries of the recency slice only; the operative layer, both canaries and the pinned registers are never trimmed and MIN_ENTRIES still refuses to go below the floor. Over budget here means the trim hit that floor and needs a human call. **Arming value is 92160 (90KB), set just above the measured card on purpose:** at 60KB the trim fell to the 12-entry floor and cut his recency slice by 40%, which is a voice-quality decision Shaheen makes deliberately, not a side effect of a plumbing fix. Negative-tested at install with a 1000 B budget (fired, cleared on restore). Detect-only; the builder owns the trim.
28. **user-scope skill inventory (C28, run-47 merged plan P2.1, 2026-08-23)** - `~/.claude/skills/` is entirely OUTSIDE skills-lock.json, the S7 hash sweep and every audit gate this repo owns; those all guard `.agents/skills/` (project scope). The run-47 assessment found the live consequence: graphify has sat at user scope since 2026-06-09, unpinned, unaudited, 113 releases stale, wired into every session by the global CLAUDE.md, self-installing a PyPI package from prose - and it was found by a human reading it in August, not by any mechanism. C28 inventories the directory by NAME against `state/user-skills-baseline.json` (written by `--init`) and ambers on a skill that ARRIVED or one that VANISHED. Deliberately names, not hashes: the signal is "something exists outside every baseline you have", and hashing user-scope content would imply this repo governs it, which it does not. Baselined at 29 skills 2026-08-23. Negative-tested both ways at install (no baseline -> fired; a planted probe dir -> fired; both cleared on restore).
29. **hook liveness (C29, run-47 merged plan P3.7, 2026-08-23)** - every wired hook leaves a breadcrumb (recall-metrics.jsonl, the day's typed transcript, system/lifecycle.jsonl) and until now NOTHING asserted that the breadcrumbs keep arriving. A hook that silently stops firing is invisible for weeks: the voice hook already died quietly once (its own header records it), and the recall + capture hooks would fail exactly as quietly because both are fail-OPEN by design - correct for a prompt path, and precisely why their silence needs a separate watcher. Each probe carries its own window sized to how often that hook can legitimately fire. **NEVER-FIRED is worded differently from WENT-QUIET** (the C20/F-14 rule): a hook wired today has no history yet and calling that "stale" would be a lie. Negative-tested at install by back-dating a breadcrumb 30 days (fired, cleared on restore).
30. **code-map freshness (C30, run-47 merged plan P7.1, 2026-08-23)** - `scripts/code-index.js` builds a deterministic map of this repo's own code (what requires / imports / dot-sources / shells out to what, plus config-invoked edges from `.claude/settings.json`, the scheduler and the manifest) into a gitignored `system/code-graph.json`, so `/deep-audit` and #27 migrations read a map instead of fanning out agents to re-read everything. **A map is only useful while it is true, and a stale one is worse than none** - it answers confidently about code that has since moved, which is exactly the query-first design flaw that disqualified graphify. C30 is N/A when the map has never been built (an absent optional index is not drift) and AMBER when it exists with a source file more than 7 days newer. Shells out C12-style so the freshness logic has one home and this file stays network-free. Negative-tested at install by back-dating the map's embedded build stamp; note the check compares that EMBEDDED stamp, not the file mtime, deliberately, since any tool can touch an mtime.
    **What the map is and is not.** Every edge is EXTRACTED by regex from a declaration; no model inferred anything, nothing is installed, no token is spent. It is DECLARATION-level, not call-level: it answers "what depends on this file", "what would this change break" and "what is orphaned", not "which function calls which". The zero-dependency law is why (real AST parsing wants tree-sitter, a native dependency, and scripts/ has no package.json); the upgrade path, if that day comes, is a pinned venv under work/, never a dependency in scripts/. Built 2026-08-23 at 246 files / 147 internal edges, and it immediately reproduced two documented claims independently: the five write-lock mutators, and all 17 wrappers dot-sourcing close-out.ps1. `--who-uses <file>` and `--orphans` are the query surface; the orphans list is explicitly NOT a delete list, since a file can be invoked by Task Scheduler, a hook, an n8n node or by hand.

**C8 tuning (b14) assessed 2026-07-12, deliberately UNCHANGED:** the hash-based check caught three real propagation gaps in one day (the #10 D7 edit, the #08/#21 P6 rider edits) with zero false positives; the "mass-write noise" class died with the mtime version. Weakening a working check to save --init runs would be the QC's over-engineering trap.

**Not this sweep's job:** semantic/content drift (stale prose, superseded claims, duplicate topics). That is the **monthly gated /lint** (Phase 3): the checker nominates a shortlist, the LLM judges only that, Shaheen decides. Deterministic checks are ~10,000x cheaper than LLM judgment, so the script gates the judge.

## Stall watchdog (P3.8, run-47 merged plan; BUILT + PROVEN 2026-08-23, ROLLOUT DEFERRED)

`run_with_watchdog` in `scripts/lib/common.sh` (ported 2026-08-25 from `Invoke-AlexWithWatchdog` in
close-out.ps1) runs a command with a wall-clock cap, kills the process GROUP on breach (GNU timeout,
non-foreground), and sets a distinct `STALLED` flag so a hang is diagnosable rather
than mute. **Why it exists, verified before it was built:** a hung `claude -p` has exactly ONE
backstop today, the scheduler's own job time limit (`PT2H` under Task Scheduler; the systemd units carry no
RuntimeMaxSec at all), and when that fires the wrapper never reaches `close_out` - so there is no
RED push and no log line. The run
does not fail, it VANISHES. That is the "a job cannot announce its own failure" class, sitting inside
this layer's own wrappers.

Fixture-proven on four cases (clean, real failure, stall killed at the timeout, 708KB of output
without deadlocking). Two PS 5.1 traps it already handles: `Start-Process -PassThru` with redirected
output returns a permanently EMPTY ExitCode (a caller testing `-ne 0` reads a failed run as clean),
and a synchronous stream read after `WaitForExit` deadlocks on a full pipe buffer, which would turn
the watchdog into the hang it catches.

**Wired into ZERO of the 17 wrappers, deliberately.** Changing how every scheduled job invokes claude
is the highest-blast-radius edit in the run-47 plan, and a subtle quoting error surfaces days later
as a missing morning brief. Shaheen approved the rollout on 2026-08-23 and deferred it to its own
session (his choice: all 17, one at a time, with per-wrapper verification).

**Runbook: `work/18-recovery-layer/watchdog-rollout-runbook.md`** - self-contained, executable by a
cold session with no re-briefing. **Recall phrase: "wire the watchdog".** Queue row:
`watchdog-wrapper-rollout`.

## Restore doctor (P6.4, run-47 merged plan, 2026-08-23)

`node work/18-recovery-layer/restore-doctor.mjs` (add `--json` for machine output; ported 2026-08-25
from restore-doctor.ps1). The RESTORE-side sibling of `check.mjs`: the checker asks "is the running system consistent", this
asks "**could this tree become a running system**". Born from run 46's fresh-clone verdict of
FRACTURE - a clone gets the functional system and none of the identity, the skill junctions are
machine-local and silently absent, and `vault/identity.md` (the compendium a restore is supposed to
read FIRST) arrives only inside the encrypted tar. All of that is deliberate on a PUBLIC repo, and
all of it was written down as prose in a runbook, which is the one form a checker cannot read and a
panicking human is least likely to follow correctly.

Checks: soul.md and soul-core.md · vault/identity.md · skill junctions resolving (parked skills
excluded, so a deliberate unlink is never a defect) · `core.longpaths` · the five re-included
tracked system files · the regenerable local databases, reported as INFO so their absence is never
mistaken for damage · and the out-of-repo identity docs, whose path is read FROM THE MANIFEST
because a fourth hardcoded copy of that path is exactly what caused run-46's N1. Every non-OK
finding carries its own fix command. Exit 0 ready / 2 blockers / 1 doctor error. Read-only.

Negative-tested at install per the P4.4 rule this same run added: hiding `soul-core.md` produced the
warning with the rebuild command, and restoring it returned the tree to READY with zero warnings.

## Security Sweep (P5, three-plan validation, 2026-07-17 - BUILT, activation queued)
A monthly, zero-token, detect-never-repair SECURITY conscience beside the integrity sweep: `security-sweep.mjs` (own script, so check.mjs's "no network except HQ push" contract is intact). Exit 0/2/1 like check.mjs. Nine assertions S1-S9: S1 gitleaks-over-history, S2 no gitignored path tracked (the V11 assertion, monthly backstop), S3 credential-age ledger (`system/credentials-ledger.json`, gitignored; the gpg passphrase POINTS at C14, no second date - G4), S4 n8n version from the b30 deployed probe NEVER prose (+ staleness) (G6), S5 Hetzner `ss -tlnp` port baseline, S6 instance-MCP connected clients (activates after P2), S7 installed-skills hash vs skills-lock.json + skills-sources.json integrity, S8 repo visibility live-vs-`meta.repo_visibility`, **S9 personal-data scan** (`scripts/personal-data-scan.js`, added 2026-07-20: git greps the git-TRACKED tree for real NAMES [derived from vault/people/ basenames at runtime, so the script itself holds no personal data], FINANCIAL kr/SEK amounts, HEALTH data values, SE phone, and **INFRA-SECRET-PATHs** (added 2026-07-21 audit F-04: a local secret-file location like the vault-backup passphrase path in a tracked/public file - the passphrase path now lives ONLY in the gitignored credentials ledger, read at runtime; the scanner terminates the class) - the PUBLIC-repo privacy conscience that gitleaks/S1 never covers, since gitleaks looks for SECRETS not personal data; reviewed exceptions in `system/personal-data-allowlist.json` [gitignored, mirrors the .gitleaks.toml model]; born from the 2026-07-20 scan that found a cached HQ metrics dump + a runway builder hardcoding real salary/burn sat public). **Network stance: LOUDER than the daily watcher** - a CONFIGURED live source unreachable = exit 1 (never silent green); a NOT-YET-CONFIGURED assertion (no baseline, tool absent) = SETUP-NEEDED finding (exit 2). Report: `vault/projects/recovery/last-security-sweep.md`. Companion: `SECURITY-PLAYBOOK.md` (per-credential rotation, "on a public repo ROTATE do not rewrite"). **Status: the pure assertions (S2/S7/S8/S9) run live and pass (S9 clean at build 2026-07-20, 47 names watched); S3 now runs too (the PS 5.1 `TryParseExact` `[ref]` overload trap - a `$null`-init ref fails "no overload for arg count 5" - fixed 2026-07-20 with a `[datetime]::MinValue` pre-type, matching the C14 fix; it reports undated credentials as SETUP, not a throw); S1/S4/S5/S6 need setup (see the playbook's activation checklist). The scheduled job IS registered (`PersonalOS-security-sweep`, monthly 1st Monday 07:20, next 2026-08-03) since the 2026-07-18 activation, so S9 runs automatically every month (the earlier "not registered yet" line was stale)** (deliberate: keeps C7/V2 green until baselines exist); registration + manifest/schedule wiring is the queued activation step.

## Notion Integration
None by design. Recovery reports via exit codes + the Alex HQ integrity metric + the brief, not a database (design: zero-token, exit-code driven).

## Vault Structure
- Tier 1: `vault/projects/recovery/status.md` (phases, checker state) + `vault/projects/recovery/last-sweep.md` (rewritten every run).
- Tier 2: `vault/projects/recovery/{github-backup-plan,vault-backup-plan,recovery-layer-plan}.md` (the three phase runbooks).

## Vault Reads
The whole tree structure (read-only): work/, .claude/commands/, vault/projects/, all vault/**/*.md for links, CLAUDE.md routing table, scheduler/schedule.md. Never modifies anything it checks.

## Vault Writes
Only its own report (`vault/projects/recovery/last-sweep.md`) + status.md + log.md. Detect-only everywhere else.

## Connections
- **Extends:** [[projects/error-log]] (reactive → preventive), /lint (gets a gate + a schedule), the run-health rollout (scripts/lib/close-out.mjs, already on every wrapper).
- **Feeds:** [[projects/alex-hq/status|Alex HQ]] (`recovery/integrity` metric), [[projects/morning-brief/status|Morning Brief]] (Monday drift lines from last-sweep.md), [[projects/alex-ai-radar/status|Alex Radar]] (shares the Monday sweep + first-Monday retro cadence).
- **Sibling phases:** [[projects/recovery/github-backup-plan]] (Phase 0), [[projects/recovery/vault-backup-plan]] (Phase 1).

## Post-Run (mandatory)
1. `last-sweep.md` rewritten (automatic).
2. `recovery/integrity` pushed to Alex HQ (automatic).
3. New real drift → surface to Shaheen; register/fix or retire-to-archive; NEVER auto-repair.
4. Structural change made → re-run `check.mjs --init` to re-baseline.
5. status.md + log.md updated on any change to the layer itself.

## Close-Out Extras
Beyond the universal list, a recovery run verifies: the sweep wrote last-sweep.md this run; the integrity metric pushed (or logged why not); exit code matches the report (0/2/1); nothing outside last-sweep.md/status.md/log.md was modified (detect-only invariant).

## Open items
- **QA hardening DONE 2026-07-04** ([[research/recovery-layer-qa-review]], 3-agent review): 5 must-fixes applied - de-hardcoded `$repo`, fail-loud try/catch, stopped self-polluting via last-sweep.md, true log line-count (`.Count` not `Measure-Object -Line`), archive/ kept as valid link targets. Findings verified all true positives; fail-loud path self-tested (throw → RED + exit 1).
- **Check-list should-fixes DONE 2026-07-04** (item 2 of the QA board): (a) C5 now matches the real `| NN |` routing-table row carrying the work_dir, not any prose mention (0*-tolerant of zero-padded row numbers); (c) C6 strips fenced + inline code before matching, so `[[links]]` shown as examples don't count; (e) C3 now scans ALL `work/` dirs against the manifest + a `meta.known_work_folders` allowlist (`voice`, the on-demand voice loop), so a rogue non-`NN` folder is caught while legit tooling is registered; **plus** cross-tree links to real files under `work/`/`sources/`/`outputs/` now resolve (were false positives). Re-sweep after: routing 0, links 26→20.
- **Final check-list pass DONE 2026-07-05** (from the items 3+4 QA, [[research/recovery-gc-qa-review]]): (b) C6 path-style links now require the full relpath or a *unique* basename (surfaced 11 real `[[application-engine/status]]` wrong-form links, rewritten to canonical `job-pipeline/status` - not suppressed); (d) **C11 index↔disk** implemented (manifest project ↔ index.md catalog); (f) **C8 is now hash-based** (status.md + CLAUDE.md hashes vs the --init baseline), mtime-immune, so the privacy-scrub mass write / a git clone can neither false-positive nor mask it - this retired the mtime version AND the 3 "reviewed current" status.md stamps it had forced. Board reached an HONEST CLEAN (0 findings).
- **Still deferred:** the #03/#14 **naming consolidation** (canonical vault name = routing name, retiring the alias pages) - bigger refactor; the aliases hold the links today. C7 remains machine-coupled by design.
- **Phase 3 LIVE 2026-07-06 (audit step 8):** the gated monthly /lint is scheduled - job `PersonalOS-lint-monthly`, first Monday 10:00, wrapper `scripts/run-lint.sh` (checker runs FIRST as the nomination pass; exit 1 aborts the LLM pass; /lint judges only the shortlist, writes vault/projects/recovery/lint-YYYY-MM.md, proposes only). Companion probe added same day: `PersonalOS-auth-check` (Sun 19:30, scripts/auth-check.sh → HQ infra/auth_ok) so headless-auth expiry is caught before the Monday train.
- **Deprecation/GC protocol (design piece 4):** supersede-never-delete tombstones + `vault/archive/` move + strike-through routing row, for the orphan-project findings (modeling, market-pulse, opportunity-scout). Candidacy is a dumb 90-day date compare; judgment stays human.
- **Grow the check list** from real misses logged in [[projects/error-log]] (design: medium-confidence v1).
- **Log-archive procedure (BUG-19 note, 2026-07-15):** C9 (log monotonicity) high-water only ever grows (`[math]::Max`). If `vault/log.md` is ever intentionally archived or split by year, run `check.mjs --init` right after to reset the high-water; otherwise C9 ambers every Monday until re-baselined. Append-only is still the rule; this is only for a deliberate archival.
- **n8n-side drift** (live workflow vs runbook export) via a REST diff - a spike, not assumed (design unknown).
- Optional dedicated Alex HQ "integrity" glance tile (metric already flows in the recovery drill-down).
